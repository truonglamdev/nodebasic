<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/css/style.css" />
        <title>Upload User</title>
    </head>
    <body>
        <h2>Upload User</h2>
        <div class="create-user-container">
            <form action="/update-user/<%=dataUser[0].id%>" method="POST">
                <div>
                    <label>First Name</label>
                    <input type="text" name="firstName" value="<%=dataUser[0].firstName%>" />
                </div>
                <div>
                    <label>Last Name</label>
                    <input type="text" name="lastName" value="<%=dataUser[0].lastName%>" />
                </div>
                <div>
                    <label>Email</label>
                    <input type="email" name="email" value="<%=dataUser[0].email%>" />
                </div>
                <div>
                    <label>Address</label>
                    <input type="text" name="address" value="<%=dataUser[0].address%>" />
                </div>
                <div><button class="btn-submit" type="submit">Save</button></div>
            </form>
        </div>
    </body>
</html>
